import React from "react";

const AlertDiv: React.FC = () => {
          <Alert key={variant} variant={variant}>
          This is a {variant} alert—check it out!
        </Alert>
};

export default AlertDiv;
